{
  "name": "brollopsspelen",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "concurrently \"node build.js --watch\" \"browser-sync start --server .dist --port 3000 --files '.dist/**/*' --no-notify\""
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.2.1"
  }
}
